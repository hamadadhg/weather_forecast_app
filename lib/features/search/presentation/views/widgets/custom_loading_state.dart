/*
import 'package:flutter/material.dart';
import 'package:weather_forecast/core/utils/styles/style_to_colors.dart';
import 'package:weather_forecast/features/search/presentation/views/widgets/custom_top_padding.dart';

class CustomLoadingState extends StatelessWidget {
  const CustomLoadingState({super.key});

  @override
  Widget build(BuildContext context) {
    return CustomTopPadding(
      child: Center(
        child: CircularProgressIndicator(
          backgroundColor: StyleToColors.whiteColor.withAlpha(51),
          color: StyleToColors.whiteColor,
          strokeWidth: 4.0, //width(thickness) the indicator
        ),
      ),
    );
  }
}
*/
